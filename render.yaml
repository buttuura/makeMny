services:
  - type: web
    name: makemny-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: MONGO_URL
        scope: runtime
        sync: false
      - key: DB_NAME
        scope: runtime
        value: makemny
      - key: PORT
        scope: runtime
        value: 5000
